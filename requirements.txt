# Python dependencies for 2D Convolution Visualizer
# Install with: pip install -r requirements.txt

# Image processing
opencv-python>=4.0.0
numpy>=1.16.0
Pillow>=6.0.0
scikit-image>=0.15.0

# GUI (usually included with Python installation)
# tkinter - typically comes pre-installed with Python
# For Ubuntu/Debian: sudo apt-get install python-tk or python3-tk
# For macOS: usually included with official Python installer
# For Windows: included with Python installer
